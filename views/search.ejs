<!DOCTYPE html>
<html lang="en-US">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="/style.css">
  <script src="./script.js"></script>
  <title><%= title %></title>
</head>
<body>
  <h1><%= title %></h1>
  <ul>
  <% if (locals.searchResults.length > 0) { %>
    <% searchResults.forEach(result => { %>
      <li>
        <%= result.username %>
      </li>
    <% }) %>
  <% } else { %>
    <li>Sorry, there were no results</li>
  <% } %>
</ul>
  <a href="/">Return to homepage</a>
</body>
</html>